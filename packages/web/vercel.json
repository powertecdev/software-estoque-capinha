{
  "buildCommand": "cd ../.. && npm install && cd packages/web && npx vite build",
  "outputDirectory": "dist",
  "installCommand": "cd ../.. && npm install",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}